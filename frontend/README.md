# 1. Fetch all remote branches from GitHub
git fetch --all

# 2. Merge the remote 'anand' branch directly into your current 'main' branch
git merge origin/anand

# 3. Push the merged changes back to GitHub
git push origin main